# Vercel Preview Origin Smoke

Date: 2026-06-04

## Objective

Validate a stable non-production `preview` branch on Vercel with explicit preview origin policy, Preview Protection enabled, safe billing gates, public PDFs, and auth entry routes without deploying production or touching DNS.

## Branch And Deployment

- Git branch used for Preview: `preview`
- Source commit: `09646e22e1ed950bc513300b25e2378ac283314a`
- Vercel project: `infrashift-r2-recovery`
- Vercel deployment: `dpl_g4sbPaLnqtpa4meyzXfiGUE6KdPL`
- Deployment state: `READY`
- Deployment target: Preview (`target: null`)
- Stable Preview origin used for smoke: `https://infrashift-r2-recovery-diegoperezroca-4286-shift-evidence.vercel.app`
- Ephemeral deployment URL was generated by Vercel, but the smoke used the stable Preview alias aligned with origin env.

## Environment Configuration

Preview branch env was configured by category only. Values are intentionally not documented.

Core:

- Neon preview/staging `DATABASE_URL`
- `BETTER_AUTH_SECRET`
- `BETTER_AUTH_URL`
- `NEXT_PUBLIC_APP_URL`
- `PREVIEW_TRUSTED_ORIGINS`
- `ADMIN_EMAILS`

R2 preview:

- `STORAGE_DRIVER=r2`
- R2 account/endpoint configuration
- Preview bucket: `shift-evidence-preview-evidence`
- Production bucket name present for config completeness only; it was not used
- R2 preview access key
- R2 preview secret key
- `MAX_UPLOAD_SIZE_MB=50`

Billing safe-gated:

- `STRIPE_CHECKOUT_ENABLED=false`
- `STRIPE_CHECKOUT_MODE=test`
- `STRIPE_LIVE_PAYMENTS_APPROVED=false`

Not loaded:

- Stripe live secret
- Stripe live Price IDs
- Stripe live webhook secret
- Wise token
- Production R2 token
- Production database URL

## Origin Policy Result

- `PREVIEW_TRUSTED_ORIGINS`: configured.
- `BETTER_AUTH_URL`: aligned to the same stable Preview origin.
- `NEXT_PUBLIC_APP_URL`: aligned to the same stable Preview origin.
- Preview origin accepted.
- No wildcard was used.
- Random `*.vercel.app` trust remains disabled by code policy.

## Smoke Method

Smoke used an authorized Vercel Preview Protection access path. The temporary access URL is intentionally not documented.

## Public Routes

All returned `200 OK`:

- `/`
- `/shiftreadiness`
- `/vmware-to-proxmox-readiness`
- `/demo`
- `/demo/replay`
- `/demo/workspace`
- `/sample-report`
- `/pricing`
- `/support`
- `/security`

## PDFs

All returned `200 OK`, `application/pdf`, and a valid `%PDF` header:

- `/demo/reports/balanced-mid-market`
- `/sample-reports/proxmox-migration-readiness-sample-report.pdf`
- `/sample-reports/proxmox-migration-readiness-premium-sample-report-v2.pdf`

## Auth Routes

- `/dashboard`: `200 OK`, rendered the sign-in gate safely.
- `/sign-in`: `200 OK`.
- `/sign-up`: `200 OK`.

Observed:

- No `500`.
- No Better Auth origin mismatch.
- No origin-policy error copy.

## Billing Checkout

All returned `200 OK`:

- `/billing/checkout/starter`
- `/billing/checkout/professional`
- `/billing/checkout/msp`

Observed:

- Checkout remained disabled/safe-gated.
- No Stripe hosted checkout session was created.
- No payment was created.
- No order was created.
- No entitlement was granted.
- No checkout origin error was observed.

## Bank Transfer

All returned `200 OK` with manual invoice / bank transfer copy:

- `/billing/bank-transfer/starter`
- `/billing/bank-transfer/professional`
- `/billing/bank-transfer/msp`

No Wise automation was used.

## Logs Review

Vercel build logs were reviewed for the final Preview deployment.

Observed:

- Prisma Client generated successfully.
- Next.js build completed successfully.
- Deployment reached `READY`.
- Warnings only:
  - Node engine `>=22` automatic major-version warning.
  - Prisma major-version update notice.

Not observed:

- Secret values printed.
- `DATABASE_URL` printed.
- R2 secrets printed.
- Stripe secrets printed.
- Auth origin exception.
- Checkout origin exception.
- Prisma init error.
- Storage init error.

## Local Validations

Completed:

- `git diff --check`: OK.
- `npm run typecheck`: OK.
- `npm run lint`: OK.
- `npx vitest run tests/unit/trustedOrigins.test.ts tests/unit/checkoutOrigin.test.ts tests/unit/billingCheckoutArchitecture.test.ts`: 3 files, 25 tests OK.
- `npm run test:run`: 119 files, 613 tests OK.
- `npm run build`: OK with non-sensitive local dummy env.

Note:

- A Windows/Prisma file-lock issue occurred during one local build attempt after tests. It was resolved by stopping only leftover `next start` processes from this workspace and rerunning the build.

## Production Safety

- No production deployment was intentionally created for this smoke.
- No promote was run.
- No custom domain was assigned.
- No DNS was touched.
- No Hostinger setting was touched.
- No production database was used.
- No production R2 bucket was used.
- No production R2 token was used.
- No Stripe live payment was enabled.
- No Wise transfer was executed.
- No real webhook or entitlement was triggered.

## Remaining Limits

- This was an unauthenticated Preview origin smoke.
- R2 authenticated browser upload/download was not exercised in this hito.
- Stripe test-mode hosted checkout was not exercised because checkout intentionally remained disabled.
- The `preview` branch currently points at the same code commit as `main`; future preview-only docs or smoke notes can remain on `main` unless the branch needs them.

## Next Steps

Recommended:

- `AUTHENTICATED-PREVIEW-SMOKE`
- `R2-AUTHENTICATED-UPLOAD-DOWNLOAD-SMOKE-VERCEL-PREVIEW`

Later, when Stripe test keys are available:

- `STRIPE-TESTMODE-PRICE-SMOKE`
